\section{\faUsers\ 工作经历}

\datedsubsection{WeRide 感知组研发工程师}{2024.06 - 至今}

\paragraph{AEB 障碍物感知融合}
\begin{itemize}
      \item 开发基于 Kalman Filter 的多来源障碍物融合算法，满足 ASIL-B 的冗余要求。
      \item 构建不同来源的观测模型，以解耦不同来源输入的维度差异；
            利用运动外推实现时间戳对齐，
            并基于 Mahalanobis Gating 和匈牙利算法进行数据关联和更新。
      \item 根据障碍物、车道线等路面信息的几何关系识别场景，针对模型的特定缺陷进行过滤或补偿。
      \item 项目交付达到100\% UT DC\&SC 覆盖，仿真 5k hours 无误触发，通过 C-NCAP 测试。
\end{itemize}

\paragraph{LiDAR freespace 后处理}
\begin{itemize}
      \item 移除特定障碍物误检。
            针对高反射率物体（如雨天路牌）引发的感知噪声，
            通过 PCA 拟合路牌所在的平面。
            根据几何关系移除异常值，剔除可能的误检。
            在删除区域结合视觉信号进行补偿。
      \item 设计基于 grid tracking 的 freespace记忆模块，
            用于支持三点掉头等复杂场景；并能够过滤偶发的误检。
            基于盲区进出的几何关系和离散计数器控制 tracker 生命周期。
            相比 Log-Odds 累计概率有更好的可解释性和更低延迟。
      \item 设计用于车端HMI可视化渲染的 freespace 点云到 linestring 的转换算法。
            基于fs记忆模块提供的稳定结果，
            采样关键点渲染 curb 轮廓线。
\end{itemize}

\paragraph{NeuralMap 车道线后处理}
\begin{itemize}
      \item 根据下游反馈和需求开发并调整后处理功能。
            赋值分合流、车道线类型等属性。
      \item 基于  Kalman Filter 识别并平滑 crosswalk，为 HMI 提供稳定的结果。
      \item 为 Auto Labeling Pipeline 实现标注部分自车行为
            （车道居中/偏离、变道、停止线前静止/起步/穿越）
            和交互车辆行为
            （静止、加减速、切入、绕行）
            的功能。
\end{itemize}



\datedsubsection{TuSimple 研发工程师——高精地图 full time \& intern }{2022.07 - 2024.03}

\paragraph{高精地图 SDK TSMap 的维护和开发}
\begin{itemize}
      \item TSMap 是 TuSimple 的高精地图 C++ SDK。
            通过 pybind11 提供 Python 接口。
            通过 pimpl 方法提高 ABI Compatibility。
      \item 开发 Perception Drivable Map。
            提供给感知可以快速获取表达可行驶区域布尔矩阵的接口。
      \item 开发 Planning Drivable Map。
            计算 freespace 距离。利用空间相关性异步计算以满足实时性要求。
      \item 根据下游场景的使用情况进行一些优化。
            例如提供批量查询曲率接口，消除冗余计算，降低了批量查询的复杂度。
\end{itemize}

\paragraph{地图产线}
\begin{itemize}
      \item 参与用于地图量产的微服务架构的新地图产线开发。
      \item 完成大部分车道线构建相关服务的开发。
            流程输入车道线矢量数据，输出包含车道、路口、拓扑关系、物理元素关系等地图元素的母库规格数据。
            接入可观测性平台，便于快速发现和溯源问题。
      \item 维护旧地图编译工具。
            根据算法和地图需求，拓展了地图元素类型，支持表达双线型边界、非车道可行驶区域等，且保证了向前和向后兼容。
            另外通过网格吸附预处理点坐标，避免因浮点数精度问题在不同边界条件检查下产生矛盾。
\end{itemize}


