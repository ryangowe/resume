\section{\faUsers\ Work Experience}

\datedsubsection{WeRide, Perception R\&D Engineer}{2024.06 - Present}

\paragraph{AEB Obstacle Perception Fusion}
\begin{itemize}
      \item Developed a multi-source obstacle fusion algorithm based on Kalman Filter to meet ASIL-B redundancy requirements.
      \item Designed per-source measurement models to decouple dimensional differences across inputs;
            used motion extrapolation for timestamp alignment,
            and applied Mahalanobis gating with Hungarian algorithm for data association and update.
      \item Identified driving scenarios from geometric relationships between obstacles, lane lines, and other road info,
            then filtered or compensated for specific model defects accordingly.
      \item Delivered with 100\% UT DC\&SC coverage, 5k hours simulation with zero false triggers, passed C-NCAP testing.
\end{itemize}

\paragraph{LiDAR Freespace Post-processing}
\begin{itemize}
      \item Removed specific false detections.
            For perception noise caused by high-reflectivity objects (e.g., road signs in rain),
            fit the plane where the sign sits via PCA,
            removed outliers based on geometric relationships,
            and compensated deleted regions with vision signals.
      \item Designed an occupancy-grid-based freespace memory module
            to support complex scenarios with perception blind spots (e.g., three-point turns) and filter sporadic false detections.
            Split space into detectable regions vs.\ blind spots, managed tracker lifecycle based on this prior rather than pure time decay;
            replaced Log-Odds with discrete counters for better interpretability and lower latency.
      \item Designed a conversion algorithm from freespace point cloud to linestring for in-vehicle HMI curb rendering.
            Sampled nearest points by polar angle, extracted key boundary points via convex hull,
            then expanded outward with a 5m threshold on both sides to generate smooth linestrings.
\end{itemize}

\paragraph{NeuralMap Lane Line Post-processing}
\begin{itemize}
      \item Developed and tuned post-processing features based on downstream feedback and requirements.
            Assigned attributes like merge/diverge type, lane marking type, etc.
      \item Identified and smoothed crosswalks using Kalman Filter, providing stable results for HMI.
      \item Built auto-labeling features for the Auto Labeling Pipeline,
            labeling ego vehicle behaviors
            (lane centering/drifting, lane change, stationary/starting/crossing at stop lines)
            and interacting vehicle behaviors
            (stationary, accel/decel, cut-in, bypass).
\end{itemize}

\datedsubsection{TuSimple, R\&D Engineer -- HD Map (full time \& intern)}{2022.07 - 2024.03}

\paragraph{HD Map SDK TSMap}
\begin{itemize}
      \item TSMap is TuSimple's HD map C++ SDK.
            Provides Python bindings via pybind11.
            Uses pimpl idiom for ABI compatibility.
      \item Built Perception Drivable Map ---
            an interface for perception to quickly get a boolean matrix of drivable area.
      \item Built Planning Drivable Map ---
            computes freespace distance, uses spatial locality for async computation to meet real-time needs.
      \item Various downstream-driven optimizations,
            e.g., batch curvature query interface that cuts redundant computation and reduces batch query complexity.
\end{itemize}

\paragraph{Map Production Pipeline}
\begin{itemize}
      \item Worked on a new microservice-based map production pipeline for mass production.
      \item Built most lane-line construction services.
            Pipeline takes lane line vector data as input,
            outputs base-map-spec data with lanes, intersections, topology, physical element relationships, etc.
            Integrated with observability platform for quick issue discovery and tracing.
      \item Maintained legacy map compilation tool.
            Extended map element types per algorithm and map needs ---
            supporting double-line boundaries, non-lane drivable areas, etc. ---
            while keeping forward and backward compatibility.
            Also preprocessed point coordinates via grid snapping to avoid contradictions in boundary checks from floating-point precision issues.
\end{itemize}
